use std::fmt;

pub fn initialize_run_stack(program_args: &[String]) {
    // TODO: finish this.
    println!(
        "initialize the run stack for mips program with args: {:#?}",
        program_args
    );

}
